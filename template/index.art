{{extend './default'}}

{{block 'body'}}
<div class="content">
  {{@content}}
  {{each source}}
  {{if $value.path.startsWith('posts')}}
  <div>
    <a href="{{ $value.link }}">{{ $value.metadata.title }}</a>
    <div>{{@$value.content.replace(/<[^<>]+>/g, '').slice(0, 100)}}...</div>
  </div>
  <br />
  {{/if}}
  {{/each}}
</div>
{{/block}}