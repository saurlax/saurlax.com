{{extend 'default'}}

{{block 'title'}}
{{metadata.title}} - Saurlax
{{/block}}

{{block 'body'}}
<div class="content">
  <h1>{{ metadata.title }}</h1>
  <div>
    <div>Time: {{ metadata.date.toLocaleString() }}</div>
    <div>Categories: {{ metadata.categories.join(', ') }}</div>
    <div>Tags: {{ metadata.tags.join(', ') }}</div>
  </div>
  {{@content}}
</div>
{{/block}}