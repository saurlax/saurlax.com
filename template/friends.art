{{extend 'default'}}

{{block 'title'}}Friends - Saurlax{{/block}}

{{block 'body'}}
<div class="content">
  {{@content}}
  <div>$ ls friends</div>
  {{each data.friends}}
  <div>
    <a href="{{$value[1]}}">[{{$value[0]}}]</a>
    <span>{{$value[2]}}</span>
  </div>
  {{/each}}
</div>
{{/block}}