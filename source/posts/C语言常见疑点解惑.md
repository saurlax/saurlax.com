---
title: C语言常见疑点解惑
date: 2023-11-14T21:05:40+08:00
categories: [C]
tags: [C]
---


## 运算符



## 数组

```c
  int fa[] = {};
  int a[10];
  // int a[];
  // error: 不允许使用不完整的类型

  int b[2] = {};
  // int b[2] = {1, 2, 3};
  // error: 初始值设定项太多

  // 如果初始化数组但不赋值，那么数组的内容是随机的
  // 使用 {} 初始化后，没有显示声明的元素都会默认初始化为 0
```

## 二维数组

```c
  int c[][5] = {{1, 2}, {1}};
  // int c[][2] = {{1, 2, 3}};
  // error: 初始值设定项太多
  // int c[5][];
  // error: 数组不能包含此类型的元素
```

## 指针

数组名实际上是一个带有长度参数的指向数组首元素的静态指针

`指针 + 1` 实际上是 `指针 + sizeof(指针指向的类型)`

```c
  int f[] = {10, 20};
  printf("%x %d %d\n", f, f[0], *f);
  // 52bffb08 10 10
  printf("%x %d %d\n", f + 1, f[1], *(f + 1));
  // 52bffb0c 20 20
  printf("%x %d %d\n", (void *)f + 1, f[1], *(int *)((void *)f + 1));
  // 52bffb09 20 335544320
```

